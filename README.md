# project-template-statex
A project template for a new paper based only on Stata and LaTeX.

# raw data
Raw data is stored only on RWI servers, the folder is created for each project. 
Data is never synchronized.